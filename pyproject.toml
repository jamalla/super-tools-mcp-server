[project]
name = "super-tools-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["mcp", "httpx"]

[project.scripts]
super-tools = "super_tools.server:main"
